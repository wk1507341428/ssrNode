{
  "name": "ssrNode",
  "version": "1.0.0",
  "description": "My exceptional Nuxt.js project",
  "author": "kang1.wang",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "test": "cross-env NODE_ENV=testing nodemon server/index.js --watch server",
    "uat": "cross-env NODE_ENV=uat nodemon server/index.js --watch server",
    "pro": "cross-env NODE_ENV=production nodemon server/index.js --watch server",
    "build": "nuxt build",
    "build:test": "cross-env NODE_ENV=testing nuxt build",
    "build:uat": "cross-env NODE_ENV=uat nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.3.6",
    "cross-env": "^5.2.0",
    "koa": "^2.6.2",
    "koa-bodyparser": "^4.2.1",
    "koa-router": "^7.4.0",
    "nuxt": "^2.4.0"
  },
  "devDependencies": {
    "nodemon": "^1.18.9",
    "eslint-config-prettier": "^4.1.0",
    "eslint-plugin-prettier": "^3.0.1",
    "prettier": "^1.16.4"
  }
}
